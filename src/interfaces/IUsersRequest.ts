/**
 * This interface is for setting the user for the request to the server
 */
export interface IUsersRequest {
  department: string;
  email: string;
  lastname: string;
  name: string;
  password: string;
}
